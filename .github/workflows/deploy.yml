name: Deploy to VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Decode SSH Key
        run: |
          echo "${{ secrets.VPS_SSH_KEY }}" | base64 -d > ./deploy_key
          chmod 644 ./deploy_key

      - name: Execute Deployment Script via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_IP }}
          username: root
          key_path: ./deploy_key
          port: 22
          script: |
            cd /var/www/hvac-erp
            git fetch origin main
            git reset --hard origin/main
            chmod +x /var/www/hvac-erp/deploy.sh
            /var/www/hvac-erp/deploy.sh

      - name: Cleanup SSH Key
        if: always()
        run: rm -f ./deploy_key